# Local dev-only test data -- not part of the submission image (the
# runner mounts /input and /output itself; see run_inference.py). input/
# only lives under workspace/ (this build context) for local docker.sh
# testing -- see submission.py's setup_action.
input/
.temp/

Dockerfile
.dockerignore
apptainer.def
docker.sh
submission.zip

**/__pycache__/
**/*.pyc
