# Local docker.sh test data (see submission.py's setup_action), not part
# of the submission image -- the runner mounts /input itself.
input/
.temp/

Dockerfile
.dockerignore
apptainer.def
docker.sh
submission.zip

**/__pycache__/
**/*.pyc
