# torch/torchvision come from the base image (pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime,
# see Dockerfile) and are deliberately not listed here -- these versions all
# satisfy that torch, so pip installs them without touching it.
nnunetv2==2.8.1
lightning==2.6.5
omegaconf==2.3.1
Pillow>=10.0
